{% extends 'base.html.twig' %}

{% block title %} {{ 'title.reset_email_send'|trans }} {{ parent() }}
{% endblock %}

{% block body %}
	<section class="container section pt-5">
		<div class="row justify-content-center">
			<div class="col-md-6 shadow rounded-3 py-3 py-sm-4 mb-2">
				<p> {{ 'resetToken.content'|trans }} {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
				</p>
				<p>{{ 'action.retry_reset_password'|trans({'link': path('app_forgot_password_request') })|raw }}</p>
			</div>
		</div>
	</section>
	{% include "base/_brand.html.twig" %}
{% endblock %}
