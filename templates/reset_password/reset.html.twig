{% extends 'base.html.twig' %}

{% block title %} {{ 'title.reset_password'|trans }} {{ parent() }}
{% endblock %}

{% block body %}
	<section class="container section pt-5">
		<div class="row justify-content-center">
			<h1 class="section-title h2">{{ 'title.reset_password'|trans }}</h1>
			<div class="col-md-6 shadow rounded-3 py-3 py-sm-4 mb-2">{{ form_start(resetForm) }} {{ form_row(resetForm.plainPassword) }}
				<button class="btn btn-primary">{{ 'action.reset_password'|trans }}</button> {{ form_end(resetForm) }}
			</div>
		</div>
	</section>
	{% include "base/_brand.html.twig" %}
{% endblock %}
